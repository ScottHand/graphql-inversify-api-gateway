handler: src/movie-handler.handler
events:
  - http:
      cors:
        origin: '*'
        headers:
          - Content-Type
      path: /movie
      method: post

memorySize: 1000
timeout: 30
reservedConcurrency: ${opt:reservedConcurrency}
